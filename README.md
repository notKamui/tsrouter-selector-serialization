Check the `src/routes/index.tsx` and see that there is a typescript error mentioning that Date is not serializable
(because it has functions), even though it is. This is also reported even if a serialization adapter is implemented.